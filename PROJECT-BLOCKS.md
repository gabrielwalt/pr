## Block Inventory

*Content structure authored (GATE 1 pending); styling not yet validated (GATE 2). Section styles: `light`/`highlight`/`dark` (foundation).*

| Block | Variant | Origin | Content model | Used on | Status |
|-------|---------|--------|---------------|---------|--------|
| `projects` | `grid` (+ typographic cover) | boilerplate `cards` → renamed `grid` → **renamed `projects`** 2026-07-09 | rows of [cover cell] + [title/desc/tag `<p>`s] → `<ul>`/`<li>`; classes `projects-card-image`/`projects-card-body`. **Cover cell (first cell) accepts a picture OR text** — author-typed text becomes a typographic cover (`.projects-card-cover`: black card, white Univers-bold display text). Lets authors fabricate a cover for an image-less entry in a snap. Empty first cell → no cover. **TWO layouts, same card, identical 351px column width:** (a) **base = horizontal scroller** (Projects page) — the `<ul>` is `display:flex; flex-wrap:nowrap; overflow-x:auto`, each `li` is `flex: 0 0 calc((100% − 3·--space-s)/4)` (4 in view, rest overflow **right and are clipped at the white content column's right edge** since the block's box = the section's capped `> div`); (b) **`.grid` variant** (Home index) — `<ul>` is `display:grid; repeat(4, minmax(0,1fr))` and **wraps** onto new rows. Mobile: grid → 2 cols; scroller → `flex-basis:66%` (~1.5 cards peek). | Home (index grid, `.grid`), Projects (scroller), Project Detail, About | ✅ structure / ✅ style (Figma-matched; lib page `/library/blocks/projects`) |
| `search-table` | — | **new** 2026-07-08 (was `index-table`) | authored = 7-col `<table>` only; JS injects `.search-table-search` (input+Clear) and live-filters rows; search bar + column-header pin on scroll (Beat 4) | Home (bottom), Search page (shared) | ✅ structure / ✅ style (13px uppercase grey, 1px borders on **all four sides** incl. left/right edge cells, sticky header; lib page `/library/blocks/search-table`) |
| `hero` | `hero-cover` | boilerplate `hero` + new variant | [picture] + [metadata ribbon `<p>` list, 2× `<h1>`] full-bleed cover; sticky reveal (Beat 1) | Home | ✅ structure / ✅ style (metadata ribbon, 80px title, real spaceship bg image; lib page `/library/blocks/hero`) |
| `event-list` | — | **new** 2026-07-08 (Calendar) — GATE 1 pending | rows of 3 cells: [metadata ribbon `<p>` list] + [date `<p>` + title `<p>`] + [thumbnail picture]. Large full-width event rows. | Calendar (`/calendar`), Project Detail ("upcoming events" excerpt) | 🔲 structure authored / 🔲 style (block JS+CSS not yet created — 404 expected until Phase 2) |
| `project-header` | — | **new** 2026-07-08 (Projects) — GATE 1 pending | 2 cells: [metadata ribbon `<p>` list] + [title `<h2>`/`<h1>` + author `<p>`]. Section intro header preceding a `projects` block of that project's entries. **Styled 2026-07-09** (Figma node `I1:13697;…;3157:180047`): flex column, pad `--page-padding` 0 `--space-m` (40/0/20; horizontal gutter from the section column), `--space-m` gap. JS labels the two cells `.project-header-meta` / `.project-header-title`. **Ribbon** = grey `--border-color` bordered cells with shared −1px dividers, 13px `--metadata-color` +0.1em tracking, ellipsis-clipped (same look as hero ribbon but block owns its own CSS — no shared rule touched). **Title** = two 42px display lines: project name `h2` bold (−0.42px tracking), collaborators `p` regular (−1.26px). Mobile: ribbon stacks vertically. | Projects (`/projects`, repeated per project), Project Detail (top, as `<h1>`) | 🔲 structure authored / ✅ style |
| `masthead` | — | **new** 2026-07-08 (About) — GATE 1 pending | one cell holding label/value pairs (`<p>`role + `<p>`name), laid out as a multi-column credits grid. | About (`/about`) | 🔲 structure authored / 🔲 style |
| `columns` | — (in `section-dark`) | boilerplate `columns` | 2-col [text+CTA] + [picture]; dark via section style | Home (Issue banner) | ✅ structure / ✅ style (42px heads w/ per-line tracking −0.42/−1.26px, 32px credits, inverted CTA; **section height driven by the TEXT column** — the image col is `position:relative;overflow:hidden` with the img `position:absolute;inset:0;object-fit:cover;object-position:top`, so the tall mockup's intrinsic aspect no longer inflates the row; it crops from the top. **Asymmetric top/bottom by direction:** the image's TOP keeps the 40 inset (symmetric with its right inset), but its BOTTOM runs past the section's 80px bottom padding to the section's bottom EDGE — via `margin-bottom: calc(-1 * var(--space-2xl))` on the img col (cancels the section pad-bottom for the image only; the text column still ends 80 above the edge). **Symmetric top/right insets (Figma 1:13138):** the dark section carries `--page-padding 0 --space-2xl` (40/0/80), so the image's top gap == its right gap (both 40 = `--page-padding`) — NOT the old zeroed-padding/edge-to-edge approach. **Headings:** "Issue 1" bold (`--weight-bold`), "Fall 2026" lighter (`--weight-regular`) per direction. Column gap `--space-s` (12), heading→body `--space-xl` (48), desc→CTA `--space-l` (36), stacked-heading gap `--space-m` (20); all from the tokenized scale. Styled additively under `.section.dark`) |

**Section styles:** `light`/`highlight` (grey surface), `dark` (black/white inversion + inverted CTAs) — foundation.

**White sheet on black — the global page frame (ALL pages).** The page ground is **black** on `html` + `body`; the readable content sits on a centered **white "sheet"** with black filling the side gutters at wide viewports (per Figma). Mechanism (`styles.css`): each non-`dark` section paints white ONLY on its inner `> div` (the `--content-max-width`-capped, centered content column), so the section itself is transparent and the black ground shows in the gutters. Consecutive white sections stack flush (the section element's own padding is 0), so the columns read as one continuous sheet. **Uniform section inset:** every `main > .section > div` carries `padding: var(--page-padding)` (40 on ALL four sides — same value as the left/right gutter), so top/bottom breathing room matches the horizontal gutter on every page and section (no more per-first/last-child or per-block vertical-pad rules). The **header** completes the sheet's top edge: `header .nav-wrapper` is transparent and the white band lives on the capped `nav` (`header.css`), which is capped to the same outer footprint (`--content-max-width` + 2×`--page-padding`) and centered — so the header's white band aligns column-for-column with the content and butts it flush (verified About @1728: header band + first section both 104→1624 px, meeting at y=104). **Home exception:** the grid section that carries the relocated nav zeroes only its `> div` top padding (`body.homepage main > .section:first-child + .section > div { padding-top: 0 }`) so the nav meets the hero flush; the other three sides keep the 40 inset.

**Home is the animated case of that same frame.** On Home the sheet *slides over the hero* rather than sitting statically: the page ground is black (`body.homepage`, `main`), the hero is a full-bleed sticky cover at `z:0`, and the grid sheet rises over it. Its scroll choreography is scoped to `body.homepage` and owns Home's sheet (the generic `body:not(.homepage)` sheet rules above are excluded there). The rising white sheet must cover the hero *exactly*: the sheet's `> div` is content-box (`--content-max-width` + 2×`--page-padding` = 1520px outer), so the hero column (`.hero-wrapper`) is capped to the same outer footprint via `max-width: calc(var(--content-max-width) + 2 * var(--page-padding))`, NOT just `--content-max-width` — otherwise the hero is 80px narrower than the sheet and isn't covered. Verified at 1728: hero-wrapper and grid sheet both 1520px, 104px black margins each side.

**Home hero touches the top edge:** the empty `header` block reserves `--nav-height` (99px) above `main`, and sections carry `--section-padding` top. On Home the hero must sit flush at the viewport top (nav overlays it, per Figma). Fixed by pulling the first section up under the header: `main:has(.hero-cover) > .section:first-child { margin-top: calc(-1 * var(--nav-height)); padding-top: 0 }`. Verified hero top = 0px. When the nav block lands, confirm it overlays (not displaces) the hero.

| `header` | Prada editorial nav | boilerplate `header` restyled | **all nav text is grey `#707070` at 37px** (Figma fills the brand grey too, NOT black). Three groups, spread across the bar: brand "Prada Index" (bold, left) — **Calendar / Projects / search-icon** cluster (mid) — **About** (flush right). Grouping matters: the search icon is a `<li class="nav-search">` inside the Calendar/Projects `<ul>`, NOT with About; About is its own trailing `<ul>` in the third fragment region. **Aligned to the grid block's 4-column grid** (Figma): the desktop nav is itself a `grid-template: auto / repeat(4, minmax(0,1fr))` with the same `--space-s` (12) gutter as the `grid` block, so its columns land at the identical 351px tracks. **The nav styling is universal — identical on every page, not Home-specific.** The desktop `header nav` is capped to the content column on ALL pages (`max-width: calc(--content-max-width + 2*--page-padding)`, `margin: auto`) so its 4 tracks align to the page content everywhere; the ONLY Home difference is *placement* (the whole `<header>` is relocated into the 2nd section — see choreography below), and there the cap is inert because the relocated `.nav-placed` nav already sits inside the narrower capped grid column. Brand spans columns 1–2 (`grid-column: 1/3`), the Calendar/Projects/search cluster + About both span columns 3–4 (`grid-column: 3/5`) with `justify-self: start` / `end` respectively — cluster left, About flush-right. **Gotchas (all three needed or tracks skew):** (1) `minmax(0,1fr)` not `1fr`, else nowrap text inflates a track; (2) `min-width:0` on `header nav > *`, same reason; (3) the mobile `grid-template` NAMED AREAS must be cleared in BOTH `header nav` AND the higher-specificity `header nav[aria-expanded='true']` desktop rules — otherwise `.nav-brand{grid-area:brand}` etc. keep snapping to the mobile areas and ignore the column placement. **Top hairline rule is SPLIT into two segments** (Prada editorial signature, per Figma node 1:13100): a short segment over the "Prada Index" wordmark, a gap, then a segment spanning the nav cluster (Calendar…About) to the right edge — NOT one continuous line. Drawn with `nav::before` (brand segment) + `nav::after` (cluster segment); the break x's come from `--rule-brand-end` / `--rule-cluster-start` CSS vars that `header.js` measures on load + resize (NOT scroll — keeps native scroll smooth), with a 20px gap each side of the break. Fallback to a full rule if the vars aren't set. | all pages | ✅ structure / ✅ style (search icon via `mask` on `/icons/search.svg`, 30px; About sized via explicit `.nav-tools ul` font-size — the container font-size doesn't reach the `<ul>`; Home sticky choreography = native: header.js relocates the whole `<header>` into the top of the grid sheet + `.nav-placed` pins it with CSS `position:sticky` (smooth, sheet-width), no JS transform) |

**Home scroll choreography** — ALL FOUR BEATS DONE (verified in preview):
- **Beat 1** (hero `sticky` reveal): `styles.css` `main:has(.hero-cover)` — hero pins, white grid rises over it.
- **Beat 2** (nav rides up + pins) — **pure native scroll, NO JS transform.** `header.js` relocates the whole `<header>` into the top of the grid sheet (`gridSection > div` — the capped content column) via `prepend`, and adds `.nav-placed`. Because the header is now real DOM content *inside* the sheet, it (a) scrolls **smoothly** with the sheet as the sheet rises over the hero (no per-frame `requestAnimationFrame` transform to stutter), and (b) is **exactly as wide as the sheet** (inherits the 1440px capped column, not viewport-wide). `header.css .nav-placed { position: sticky; top: 0; z:4 }` pins it natively; a white `background-color` covers the cards scrolling beneath. Its inner `nav` drops its own horizontal page-padding (the sheet's `> div` already supplies the 40px gutter) to avoid doubling. **Why relocate rather than fixed-position:** a `position:fixed` + JS-`translateY` nav can't scroll as smoothly as native content and spans the whole viewport (wider than the sheet) — the user flagged both; moving it into the DOM fixes both at once.
- **Beat 3** (dark COVERS nav — a REVEAL): the dark Issue section rises on a **higher stacking layer than the pinned nav** (`z:5 > nav z:4 > grid z:1 > hero z:0`; the z:5 rule is `main:has(.hero-cover) > .section:first-child ~ .section.dark` — the `:first-child ~` shape is required so it out-specifies the generic `~ .section { z:1 }` rule, else it ties and loses). With the nav now native-sticky inside the grid section, the dark section simply covers the entire grid section (nav included) as it rises — no eviction class or visibility toggle needed; when the grid section scrolls off, its sticky nav goes with it. **Gotcha:** an even earlier build slid the nav off-screen with a negative transform (a slide-off, not a cover); the spec requires the higher-layer dark section to visibly cover it.
- **No black gap at the hero/sheet seam** — TWO section paddings had to go, both from `main > .section { padding: var(--section-padding) 0 }`: (1) the grid sheet's `padding-top` (its white ground lives on the inner `> div`, so the section's own top pad showed the black ground above the nav) → `main:has(.hero-cover) > .section:first-child + .section { padding-top: 0 }`; (2) the **hero's own `padding-bottom`** — the full-bleed hero image ends at the inner `> div` but the section's 30px bottom pad extended the sticky hero below it, showing as a black band before the white sheet. Fixed by zeroing the hero's vertical padding entirely: `main:has(.hero-cover) > .section:first-child { padding-block: 0 }`. Verified hero image bottom == sheet top, gap 0.
- **Beat 4** (table header pin): built into `search-table.css`.
- `prefers-reduced-motion`: no JS motion controller is involved anymore (relocation is a one-time DOM move, not scroll-driven); the nav pins via plain CSS `sticky`, which is acceptable under reduced-motion. On Home the empty `<header>` landmark reserves no flow space (`min-height:0`).
- **Gotcha (opaque cover):** every post-hero section must paint an **opaque** ground across its FULL width, because the sticky hero sits at `z:0` *above* `main`'s own black background — so a `transparent` section reveals the hero through its side gutters (not `main`'s black). Fix: `main:has(.hero-cover) > .section:first-child ~ .section { background-color: var(--text-color) }` (black sides for ALL covering sections incl. dark), then `…:not(.dark) > div { background-color: var(--background-color) }` paints white only inside the capped content column of light sections. The dark banner is thus fully opaque black edge-to-edge and covers the hero with no bleed-through.

**Nav + footer are auto-included (no per-page metadata).** `header.js`/`footer.js` resolve the fragment path themselves: `<contentPrefix>/nav` and `<contentPrefix>/footer`, where `contentPrefix` = `/content` when `window.location.pathname` starts with `/content/` (the local `aem up` server) and empty in production (root serving). An authored `nav`/`footer` metadata value still overrides if ever needed. So content `.plain.html` files carry **no** nav/footer metadata rows. Footer load re-enabled in `scripts.js` (fragment: `content/footer.plain.html`).

**Homepage is a page template, not `.hero-cover` sniffing.** Home carries a `metadata` block `template = homepage` → `body.homepage` (via EDS `decorateTemplateAndTheme`). All the bespoke Home choreography is scoped to `body.homepage main …` (was `main:has(.hero-cover)`), and `header.js` gates the nav-relocation on `document.body.classList.contains('homepage')`. A `scripts.js` fallback adds `body.homepage` when `main .hero-cover` is present (backup only, per `page-template-metadata`), so the choreography survives even if the metadata is dropped.

## One-off Registry

*[Agent: record one-off content structures here as they're approved.]*
