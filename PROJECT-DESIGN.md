## Migration Strategy

*[Agent: record the 12 orientation inputs here after running `migration-orientation`.]*

## Design Tokens

```css
:root {
}
```

## Typography

*[Agent: fill after `global-style-foundation`.]*

## Color

*[Agent: fill after `global-style-foundation`.]*

## Spacing

*[Agent: fill after `global-style-foundation`.]*

## Breakpoints

*[Agent: fill after `global-style-foundation`.]*

## Block Inventory

*[Agent: fill as blocks are built — see also `PROJECT-BLOCKS.md` for the detailed registry.]*
