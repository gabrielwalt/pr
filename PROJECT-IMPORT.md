## Figma Source

*[Agent: record the Figma file URL(s) here after orientation.]*

## Frame → Page Mapping

*[Agent: record each Figma frame/screen → EDS page path mapping here as pages are built. No import script exists on this project — the source is a Figma prototype, not a live site; content is authored per-frame via `excat-figma:excat-figma-migration`.]*

## Node ID Reference

*[Agent: record frame name → Figma node ID for quick lookup as frames are worked.]*
