| ID | Status | Priority | Files | Problem | Fix | Acceptance Criteria |
|----|--------|----------|-------|---------|-----|----------------------|
| 1 | ✅ Done | High | `PROJECT-DESIGN.md` | No migration strategy recorded yet | Run `migration-orientation` | `## Migration Strategy` section exists in `PROJECT-DESIGN.md` with all 12 inputs recorded and user-confirmed |
| 2 | ✅ Done | High | `PROJECT-IMPORT.md`, `PROJECT-TEMPLATES.md` | Figma frames not yet inventoried | Explore the full Figma document; inventory frames/screens, node IDs, and frame→page mapping | Frame inventory recorded; Home frame node ID identified; "Entry Cover Templates" variant set catalogued — all 9 links verified accessible 2026-07-08; shared Page Template (5 screens), Entry Template (Slideshow), Home/Overlay bespoke, 8 cover variants catalogued |
| 3 | ✅ Done | High | `PROJECT-DESIGN.md`, `styles/styles.css`, `styles/fonts.css`, `head.html` | No global style foundation yet | Run `global-style-foundation` from ≥3 representative frames | Brand tokens, type scale, spacing, default-content + CTA styling recorded and applied — Univers Next Pro (Adobe kit lnd2jia) wired; monochrome palette + editorial ramp in styles.css; lint clean; detect.mjs clean. Note: in-browser preview not yet verified (blocker #5). |
| 5 | ✅ Done | High | preview/dev server | localhost:3000 serves a different project (Semrush), not this repo — cannot visually verify foundation or future pages | Point the local dev/preview server at this repo (`/workspace/current`) so `styles.css` + Univers render; confirm bare default content (title/body/link/CTA) renders on-brand | Restarted `aem up` from this repo (was running from deleted `semrush3` cwd). Now proxies `main--pr--gabrielwalt.aem.page`. Verified in-browser: Univers Next Pro loads, black-on-white, h1 desktop=80px/700/−2.4px, body 16/19, primary button squared+uppercase. |
| 6 | 🔲 Open | High | `content/library/`, `block-library.json` or `tools/sidekick/library.json`, `PROJECT-IMPORT.md` | No component library scaffolded yet — needed as the baseline for a consistent block/design system before Home introduces its first blocks | Scaffold `content/library/default-content` + `content/library/blocks/<block>` for whatever Home needs, per `component-library`; record the DA/UE handoff status in `PROJECT-IMPORT.md` | `/library/default-content` renders every semantic element on-brand; each Home block has a clean one-instance-per-variant library page; library location recorded in `PROJECT-IMPORT.md`; user has the Library-Handoff checklist if DA |
| 4 | 🔲 Open | High | `content/`, blocks | Home not yet built | Build Home content + blocks from Figma via `excat-figma:excat-figma-migration` | Home passes GATE 1 (content) then GATE 2 (style) — grow the component library (task 6) in the same step each new block is validated |
| 7 | ✅ Done | Medium | `scripts/aem.js` (`decorateSections`) | This enhanced boilerplate's `decorateSections` did not process `.section-metadata` into section classes, so the foundation's `.section.light/.highlight/.dark` styles never applied. Discovered scaffolding `/library/sections`. | Port the standard EDS section-metadata decoration (read `.section-metadata` rows → `toClassName` → add classes to the `.section`, then remove the metadata div) into `decorateSections` | Verified in preview 2026-07-08: `/content/library/sections` renders Light on grey `#f5f5f5` and Dark inverted (white-on-black) with white-fill CTA; `.section.dark`/`.section.light` classes present. ESLint clean. |
